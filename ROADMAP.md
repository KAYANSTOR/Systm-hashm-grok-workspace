# خطة الطريق إلى الإنتاج — نظام معامل هاشم الأحمدي

> يكمّل هذا الملف `PROJECT_STATUS.md` (الحالة الحالية). هذا الملف يحدد **الخطوات التالية بالترتيب** حتى إطلاق النظام رسميًا للاستخدام اليومي في المعمل.
> آخر تحديث: 11 سبتمبر 2026

مبدأ الترتيب: **الأمان والبيانات أولًا، ثم الاستقرار، ثم الجاهزية الميدانية، ثم الصقل.** كل مرحلة يجب أن تنتهي بـ typecheck + test + build ناجحين قبل الانتقال للتي تليها.

---

## المرحلة 0 — تصفير الديون التقنية (منجزة جزئيًا اليوم)

- [x] حذف الملفات المتناثرة والتجريبية من الجذر
- [x] إزالة `firebase-applet-config.json` ومفتاح API المكشوف
- [x] إزالة تبعيات Firebase غير المستخدمة
- [x] تحديث `.gitignore` لمنع تسرب ملفات الكاش
- [x] توحيد ملف الحالة في `PROJECT_STATUS.md`
- [ ] حسم ملف قفل التبعيات (npm أو bun — احذف الآخر)
- [ ] سد فجوة ترقيم الهجرة `0009` أو توثيق سبب غيابها
- [ ] قرار بشأن `AGENTS.md` / `metadata.json` / `screenshots/` (إبقاء، تخصيص، أو حذف)

**معيار الإنجاز:** مستودع خالٍ من الملفات الميتة، بمصدر حقيقة واحد لحالة المشروع.

---

## المرحلة 1 — الأمان قبل أي شيء (حرجة، يجب إنجازها قبل فتح النظام لمستخدمين حقيقيين)

1. **تدوير مفتاح Firebase المكشوف سابقًا** في لوحة Firebase Console (حتى بعد حذفه من الكود، ظل مرئيًا في تاريخ Git العام — يُنصح باعتباره مخترقًا وإصدار مفتاح جديد إن كان المشروع لا يزال يستخدم أي خدمة Firebase فعليًا؛ إن لم يكن يُستخدم إطلاقًا، احذف مشروع Firebase نفسه).
2. **تدقيق كامل لسجل Git بحثًا عن أسرار أخرى** مكشوفة تاريخيًا (مفاتيح API، كلمات مرور، DATABASE_URL) — ولو أُزيلت لاحقًا، فهي لا تزال في التاريخ ما لم يُعاد كتابة السجل.
3. **ضبط متغيرات البيئة في Vercel** (يدوي من طرفك):
   - `VITE_AUTH_ENABLED=true`
   - `BETTER_AUTH_SECRET` (قيمة عشوائية قوية، غير المستخدمة في التطوير)
   - `BETTER_AUTH_URL` (نطاق الإنتاج الفعلي)
   - `DATABASE_URL` (اتصال Neon الإنتاجي)
4. **مراجعة صلاحيات كل مسار API** والتأكد أن كل عملية كتابة (فاتورة، سند، صرف مخزني) تتحقق من دور المستخدم على مستوى الخادم لا الواجهة فقط.
5. **نسخ احتياطي تلقائي لقاعدة Neon** (جدولة يومية) قبل فتح النظام لبيانات حقيقية.

**معيار الإنجاز:** لا أسرار مكشوفة، بيئة إنتاج مضبوطة بالكامل، نسخ احتياطي فعّال.

---

## المرحلة 2 — تثبيت الاستقرار الوظيفي

1. تشغيل سيناريو تعارض المزامنة يدويًا (جهازان يعدّلان نفس الفاتورة أوفلاين ثم يتصلان) والتأكد أن واجهة حل التعارض تعمل كما هو مصمم.
2. اختبار ميداني حقيقي على هاتف أمين المخزن: قطع الإنترنت فعليًا، تنفيذ عمليات صرف/استلام، إعادة الاتصال، والتأكد من عدم فقد أي حركة ووصولها للـ ledger بشكل صحيح.
3. توضيح مصير `p2p.ts`: إما دمجه فعليًا في تدفق العمل (مثلاً كقناة مزامنة بديلة عند غياب الإنترنت الكلي) أو حذفه إن كان تجريبيًا متروكًا.
4. رفع تغطية الاختبارات لمسارات الصلاحيات الحرجة (من يمكنه اعتماد فاتورة، من يمكنه حذف سند) إن لم تكن مغطاة بالكامل.
5. تنظيف تحذيرات lint الـ38 المتبقية (متغيرات غير مستخدمة) — لا تؤثر وظيفيًا لكنها تُبقي الكود نظيفًا لأي مطوّر/وكيل يعمل عليه لاحقًا.

**معيار الإنجاز:** سيناريوهات العمل الحقيقية (أوفلاين → أونلاين، تعارض، صلاحيات) مُختبرة يدويًا ومؤتمتة قدر الإمكان.

---

## المرحلة 3 — الجاهزية الميدانية والتشغيلية

1. **تدريب المستخدمين الفعليين** (المدير، المحاسب، أمين المخزن) على الأدوار والصلاحيات الجديدة قبل الإطلاق الكامل.
2. **خطة ترحيل البيانات القديمة** إن وُجدت بيانات سابقة (فواتير/عملاء/مخزون) يجب استيرادها لمرة واحدة إلى الهيكل الجديد.
3. **مراقبة الأخطاء في الإنتاج**: ربط أداة تتبع أخطاء (مثل Sentry أو ما يعادلها) لرصد أي فشل في المزامنة أو الطباعة بعد الإطلاق مباشرة.
4. **خطة رجوع (Rollback)**: توثيق خطوات العودة لنسخة سابقة من التطبيق ومن قاعدة البيانات في حال ظهور مشكلة حرجة بعد النشر.
5. تثبيت PWA فعليًا على هاتف/أجهزة المستودع والتأكد من التحديث التلقائي عند نشر نسخة جديدة (لا يبقى المستخدم عالقًا على نسخة قديمة من Service Worker).

**معيار الإنجاز:** فريق العمل مدرَّب، البيانات مُرحّلة، وهناك خطة واضحة لو حدث خطأ بعد الإطلاق.

---

## المرحلة 4 — الإطلاق والصقل بعد الإطلاق

1. إطلاق تدريجي: تشغيل النظام بالتوازي مع الطريقة القديمة (يدوية/إكسل) لمدة أسبوعين قبل الاعتماد الكلي.
2. جمع ملاحظات المستخدمين الفعليين وترتيبها حسب الأثر (خلل حرج > إزعاج > تحسين تجميلي).
3. مراجعة أداء التطبيق تحت حمل حقيقي (سرعة تحميل التقارير، حجم قاعدة البيانات بعد أسابيع من الاستخدام الفعلي).
4. تحديث `PROJECT_STATUS.md` رسميًا بعد الإطلاق ليعكس "تم الإطلاق الإنتاجي" كنقطة مرجعية، وبدء دورة تحسين مستمر.

**معيار الإنجاز:** النظام هو مصدر العمل الوحيد فعليًا في المعمل، بدون اعتماد على الطريقة القديمة.

---

## ملخص الأولوية الفورية التالية (ابتداءً من اليوم)

1. [x] إصلاح تعذّر الوصول لصفحة استرداد الحساب `/recovery` (كانت محجوبة خلف بوابة الدخول نفسها — انظر `PROJECT_STATUS.md` §4.5).
2. دفع تنظيف هذه الجلسة + إصلاح الدخول إلى GitHub.
3. **التحقق من صلاحية رمز الطوارئ في migration 0023 (ساعتان فقط)** — إن انتهت، وَلِّد رمزًا جديدًا فورًا حتى يستفيد أي موظف مقفَل من صفحة الاسترداد التي أصبحت الآن قابلة للوصول فعليًا.
4. تدوير/حذف مفتاح Firebase المكشوف تاريخيًا في Git.
5. ضبط متغيرات بيئة Vercel للمصادقة.
6. اختبار ميداني حقيقي واحد على الأقل لسيناريو الأوفلاين.
