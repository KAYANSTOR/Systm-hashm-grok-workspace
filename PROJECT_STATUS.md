# حالة المشروع — نظام إدارة معامل هاشم الأحمدي للتصميم والتطريز

> آخر تحديث: 11 سبتمبر 2026
> هذا الملف هو **المرجع الوحيد** لمعرفة أين وصل التطوير. يُحدَّث في نهاية كل جلسة عمل رئيسية بدل إنشاء ملفات ملخص جديدة متفرقة.
> **لكل وكيل يعمل على هذا المستودع:** الخطة الملزمة للخطوات التالية موجودة في `ROADMAP.md` — التزم بترتيب مراحلها ولا تخرج عنها دون طلب صريح من صاحب المشروع (التوجيه الكامل مكرر في أعلى `AGENTS.md`).

---

## 1. نظرة عامة

نظام لإدارة عمليات معمل تصميم وتطريز (مبيعات، مخزون، صندوق، سندات، مصروفات، تقارير، موظفون وصلاحيات) مبني ليعمل فعليًا في الإنتاج مع دعم العمل دون اتصال إنترنت (للمستلم في المستودع مثلًا) ومزامنة سحابية.

**المكدس التقني:** React 19 + TanStack Start + Tailwind + Zustand · Postgres/Neon (سحابي) + PGlite (محلي) · Drizzle ORM · Better Auth · نشر على Vercel.

**المستودع:** `KAYANSTOR/Systm-hashm-grok-workspace`

---

## 2. الوحدات المكتملة فعليًا (تم التحقق من الكود لا من الوصف فقط)

| الوحدة | الحالة | ملاحظات |
|---|---|---|
| المزامنة دون اتصال (Outbox + Sync Engine) | ✅ منجزة | IndexedDB outbox حقيقي، عمليات SQL idempotent عبر `ON CONFLICT` |
| دفتر حركة المخزون (Ledger) | ✅ منجزة | الكمية مُشتقّة من `inventory_movements` وليست حقلًا قابلًا للتعديل المباشر |
| أمر الصرف المخزني والتسوية | ✅ منجزة | موجهة عبر الـ ledger |
| المصادقة (Better Auth) | ✅ منجزة وفعّالة | تسجيل دخول برقم الهاتف/كلمة مرور، جلسات خادم، عزل بيانات |
| الأدوار والصلاحيات (مدير/محاسب/أمين مخزن) | ✅ منجزة | صفحة `settings.access-control.tsx` + جداول صلاحيات مخصصة |
| إدارة الموظفين ودورة حياة الحساب | ✅ منجزة | تفعيل/تعطيل حسابات، migration `0018` |
| حل تعارضات المزامنة | ✅ منجزة | `sync-conflict.ts` + جدول `sync_conflicts` |
| قوالب الطباعة/PDF/مشاركة واتساب | ✅ منجزة | فاتورة/سند، كشف حساب، تقارير مخزون ومصروفات — كلها بلون `#088c94` وخط Cairo واتجاه RTL |
| PWA وتشغيل دون اتصال | ✅ منجزة | Service Worker يعمل فعليًا (48 ملف مخزَّن مسبقًا عند البناء) |
| النشر | ✅ فعلي على Vercel | مشروع `systm-hashm-grok` |
| الاختبارات الآلية | ✅ 67 اختبارًا ناجحًا | `npm test` — 22 مجموعة اختبار |
| فحص الأنواع والبناء | ✅ نظيفان | `npm run typecheck` و `vite build` بدون أخطاء |

**وحدة غير موثّقة سابقًا تستحق المراجعة:** `src/lib/multiplayer/p2p.ts` (24K) — وحدة اتصال نظير-لنظير موجودة في الكود لكن دورها ضمن تدفق العمل الفعلي غير موضّح بعد؛ يُنصح بمراجعتها أو توثيقها أو حذفها إن كانت تجريبية غير مُستخدمة.

---

## 3. أعمال التنظيف المنجزة في هذه الجلسة (11 سبتمبر 2026)

تم تنفيذ الفحص والتنظيف والتحقق فعليًا على نسخة محلية من المستودع، مع تشغيل `typecheck` و`test` و`lint` و`vite build` بعد كل تعديل للتأكد من عدم كسر أي شيء (كل الفحوصات نجحت: 0 أخطاء).

### حُذفت (ملفات تجريبية/بقايا جلسات سابقة، لا يستخدمها الكود الفعلي):
- `test.js`, `test.ts`, `test2.ts`, `test3.ts` — سكربتات تجريب يدوية لـ zustand/store
- `patch_store.ts`, `patch_store_2.ts` — سكربتات تعديل آلي لـ `store.ts` تم تطبيق تعديلاتها فعليًا سابقًا (تحقّقنا من وجودها في الكود الحالي) وبقيت بلا فائدة
- `clear_db.ts` — سكربت يحذف كل بيانات قاعدة البيانات الإنتاجية دون أي حماية؛ خطر أمني لو نُفّذ بالخطأ
- `test-store.ts` — سكربت تجريب hydration
- `UPLOAD_INSTRUCTIONS.md` — تعليمات رفع يدوي من مرحلة سابقة (تشير لمigrations قديمة 0004-0006 بينما وصلنا الآن لـ 0018)
- `DEVELOPMENT_SUMMARY_AR.md` — استُبدل بهذا الملف (`PROJECT_STATUS.md`) لتفادي وجود ملفَي حالة متعارضين
- `firebase-applet-config.json` — **كان يحتوي مفتاح Firebase API مكشوفًا في مستودع عام**، وتبيّن أن Firebase غير مُستخدم إطلاقًا في `src/` (لا استيراد واحد)
- تبعيتا `firebase` و `firebase-admin` من `package.json` (وتحديث `package-lock.json` تبعًا لذلك — تقلّص بأكثر من 3100 سطر)
- `artifacts/.tmp/mcp-results/*.json` (136K) — ملف كاش أدوات تم Commit بالخطأ
- `test-results/.last-run.json` — كاش Playwright تم Commit بالخطأ

### حُدّثت:
- `.gitignore` — إضافة `artifacts/`, `test-results/`, `playwright-report/`, `.output/`, `.nitro/` لمنع تكرار تسرّب ملفات الكاش مستقبلًا
- `eslint.config.mjs` — إزالة استثناء `patch_store*.ts` غير اللازم بعد الحذف

### لوحظ ويحتاج قرارًا منك (لم يُغيَّر تلقائيًا لتجنّب أي مخاطرة):
- **يوجد ملفا قفل تبعيات معًا**: `package-lock.json` و `bun.lock`. هذا يسبب لبسًا حول مدير الحزم الفعلي المستخدم في الإنتاج/CI. يُفضّل حذف أحدهما بعد تأكيد أي منهما يستخدمه Vercel فعليًا.
- **فجوة في ترقيم الهجرات**: لا يوجد ملف `0009_*.sql` (القفزة من 0008 إلى 0010). على الأغلب حُذف عمدًا في وقت ما، لكن يستحق تأكيدًا سريعًا أنه لا يوجد اعتماد مفقود عليه في قاعدة الإنتاج.
- **`AGENTS.md`** (20K) في الجذر يحتوي تعليمات عامة لعامل بناء التطبيقات (Grok App Builder) أكثر من كونه توثيقًا خاصًا بهذا المشروع تحديدًا. تُرك كما هو لأن أدوات مثل Codex قد تعتمد عليه، لكن يستحق مراجعة لاحقًا لتخصيصه لهذا المشروع.
- **`metadata.json`** و **مجلد `screenshots/`**: لا يوجد أي استخدام لهما في الكود، يبدوان من بقايا سقالة "Grok App Builder" الأصلية. تُركا لعدم اليقين من اعتماد منصة الاستضافة عليهما، لكنهما مرشّحان للحذف لاحقًا بعد التأكد.

---

## 4. الحالة الصحية الحالية للكود (بعد التنظيف)

```
npm run typecheck   → ✅ 0 أخطاء
npm test            → ✅ 67/67 ناجحة (22 مجموعة)
npm run lint        → ✅ 0 أخطاء، 38 تحذير (متغيرات غير مستخدمة موجودة مسبقًا، لا علاقة لها بهذه الجلسة)
vite build           → ✅ بناء ناجح، Service Worker يُنتَج بنجاح (48 ملف Precache)
```

---

## 4.5 إصلاح حرج — تعذّر تسجيل الدخول (11 سبتمبر 2026)

**الأعراض:** المستخدم لا يستطيع تجاوز شاشة تسجيل الدخول حتى مع بيانات صحيحة/رمز استعادة طوارئ.

**السبب الجذري:** بين وقت الاستنساخ الأول وهذه الجلسة، حصلت سلسلة إصلاحات مصادقة على المستودع البعيد (تطبيع أرقام الهاتف، تفادي تحديد المعدل، حل الهويات القديمة على الخادم) انتهت بإضافة صفحة استرداد طارئة `/recovery` + جدول `account_recovery_tokens` (migration 0023). المشكلة: `__root.tsx` كان يضع **كل** الصفحات — بما فيها `/recovery` نفسها — خلف `SignInGate`، الذي عند غياب الجلسة يتجاهل المسار المطلوب تمامًا ويعرض شاشة الدخول العامة فقط. النتيجة: صفحة الاسترداد التي أُضيفت لحل المشكلة كانت **غير قابلة للوصول إطلاقًا** لأي شخص مسجَّل خروجه أصلًا — وهم بالتحديد من تستهدفهم.

**الإصلاح المطبَّق** (`src/routes/__root.tsx`):
- استثناء `/recovery` من `SignInGate` عبر فحص المسار الحالي بـ `useRouterState`، بحيث يُعرض دائمًا بغض النظر عن حالة الجلسة.
- إضافة رابط ظاهر "لا يمكنك تسجيل الدخول؟ استعادة الحساب برمز الطوارئ" داخل شاشة الدخول نفسها (لم يكن هناك أي رابط ظاهر للمستخدم يقوده لصفحة `/recovery` أصلًا، حتى لو كانت تعمل).

**تم التحقق:** typecheck ✅ · test (67/67) ✅ · lint (0 أخطاء) ✅ · build ✅.

**⚠️ ملاحظة مهمة:** رمز الطوارئ في `migrations/0023_account_recovery.sql` صالح لمدة ساعتين فقط من وقت تطبيق الهجرة على قاعدة الإنتاج. إن كانت الساعتان قد انقضتا، يجب توليد رمز طوارئ جديد (هاش SHA-256 لقيمة عشوائية تُدرَج في هجرة جديدة) قبل أن يكون الاسترداد مفيدًا فعليًا.

---

## 5. البنود المفتوحة (لم تُنجز بعد)

1. متغيرات بيئة Vercel للمصادقة: `VITE_AUTH_ENABLED=true`, `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`, `DATABASE_URL` — **يتطلب إعدادًا يدويًا منك في لوحة Vercel**، خارج نطاق ما يمكن لـ Claude فعله.
2. اختبار فعلي على جهازين حقيقيين لسيناريو انقطاع الشبكة (هاتف المستلم في المستودع) — لم يُجرَ بعد حسب تقرير التدقيق الأخير.
3. توضيح دور `p2p.ts` (انظر القسم 2).
4. حسم ملف قفل التبعيات المزدوج (npm/bun).
5. تنظيف 38 تحذير lint المتبقية (متغيرات غير مستخدمة) — منخفضة الأولوية.

للخطة التفصيلية بالمراحل حتى الإنتاج، انظر `ROADMAP.md`.


## 6 — إصلاح صفحات الموظفين والأدوار والصلاحيات (11 سبتمبر 2026)

تم تطبيق حزمة `employee-roles-pages-fix.zip` على آخر نسخة من `main`. تضمنت التغييرات إضافة `src/lib/access.ts` كمصدر مركزي لربط المسارات بصلاحيات الأدوار، تصفية القوائم والتنقل السريع حسب صلاحيات الموظف، وحماية محتوى الصفحة عند محاولة الوصول المباشر لمسار غير مسموح. كما أضيفت إرشادات تفعيل حساب المدير وتوضيح أثر حفظ صلاحيات الدور، مع تحديث إعادة تحميل البيانات بعد تفعيل المدير من صفحات الموظفين أو الصلاحيات.

تمت مطابقة أسماء الصلاحيات الجديدة مع الهجرات `0016` و`0017`، ولم تُضف صلاحيات غير معرّفة في قاعدة البيانات. سيتم تسجيل نتائج `typecheck` و`test` و`lint` و`build` بعد اكتمال التحقق قبل الرفع.


نتائج التحقق لهذه الحزمة: `npm run typecheck` ✅، `npm test` ✅ (76/76، منها 4 اختبارات جديدة لخريطة الصلاحيات)، `npm run lint` ✅ دون أخطاء مع 36 تحذيرًا قائمًا، و`npx vite build` ✅. تم الحفاظ على توثيق README السابق ودمج إرشادات الأدوار الجديدة بدلاً من استبداله بالكامل.


## 7 — إصلاح ربط بطاقة الأدوار والصلاحيات في الإعدادات (11 سبتمبر 2026)

تم إصلاح سبب ظهور بطاقة «الأدوار وصلاحيات الشاشات» وكأنها غير مرتبطة: كان حارس المسارات يعتمد على قائمة صلاحيات فارغة أثناء التحميل الأولي، فيحجب المسار `/settings/access-control` قبل وصول الصلاحيات من الخادم. أضيفت حالة `permissionsLoaded` إلى المتجر، وأصبح الحارس يسمح بالعرض المؤقت أثناء الجلب ثم يطبّق التصفية والمنع بعد اكتمال الصلاحيات. كما أصبح زر تفعيل حساب المدير يعيد جلب الصلاحيات والبيانات فورًا دون الحاجة لإعادة فتح التطبيق.

نتائج التحقق: `npm run typecheck` ✅، `npm test` ✅ (76/76)، `npm run lint` ✅ دون أخطاء مع 36 تحذيرًا قائمًا، و`npx vite build` ✅.


## 8 — تثبيت تفعيل مدير النظام وربط بطاقة الأدوار (11 سبتمبر 2026)

تم إصلاح منطق بطاقة «الأدوار وصلاحيات الشاشات» بحيث يعمل بعد تفعيل المدير فعليًا. أصبح الخادم يمنع إنشاء مدير نظام ثانٍ إذا كان هناك مدير محفوظ مسبقًا، مع إبقاء استدعاء المدير الحالي آمنًا وقابلًا لإعادة المحاولة دون تكرار الأدوار. بعد نجاح التفعيل تُعاد قراءة الصلاحيات من قاعدة البيانات وتتحول الواجهة إلى حالة «تم تفعيل مدير النظام وحفظ الصلاحيات»، ويختفي زر التفعيل المتكرر. تظل إدارة الأدوار والصلاحيات محفوظة في جداول `roles` و`role_permissions` و`user_roles`.

نتائج التحقق: `npm run typecheck` ✅، `npm test` ✅ (76/76)، و`npx vite build` ✅. فحص ESLint السابق بلا أخطاء مع تحذيرات موجودة مسبقًا.


## 9 — إصلاح تفعيل المدير مرة واحدة وربط بطاقة الأدوار (11 سبتمبر 2026)

تم تطبيق حزمة `admin-activate-once-fix.zip` على آخر نسخة من `main` مع الحفاظ على حماية الخادم الموجودة التي تمنع إنشاء مدير نظام ثانٍ. بعد نجاح التفعيل تعيد الواجهة تحميل الصلاحيات والبيانات فورًا، وتعرض حالة مدير النظام بدل زر التفعيل المتكرر. كما أصبحت بطاقة الموظفين والأدوار تعرض روابطها فقط عند توفر الصلاحيات، وتعرض إرشادًا واضحًا للتمهيد قبل التفعيل. أُضيف في `src/lib/access.ts` استثناء تمهيدي محدود يسمح للحساب بلا صلاحيات بفتح `/settings` فقط، مع بقاء `/settings/access-control` محميًا، وأضيف اختبار لهذا السلوك.

نتائج التحقق: `npm run typecheck` ✅، `npm test` ✅ (78/78)، `npm run lint` ✅ دون أخطاء مع التحذيرات القائمة، و`npx vite build` ✅.


## 10 — اختبار وإصلاح كرت الأدوار والصلاحيات (11 سبتمبر 2026)

تم اختبار تدفق بطاقة «الأدوار وصلاحيات الشاشات» وخريطة الوصول. أظهر الفحص أن البطاقة كانت تعرض رسالة التفعيل قبل اكتمال جلب الصلاحيات، ما يجعلها تبدو معطلة أو غير مرتبطة حتى أثناء التحميل. أضيفت حالة تحميل واضحة، ووُحّد شرط إدارة الوصول في دالة `canManageAccess` تستخدمها البطاقة وصفحة الإعدادات معًا، كما أضيفت اختبارات للصلاحيات الصريحة ولحالة التحميل الآمنة. تم أيضًا إصلاح خطأ JSX ظهر أثناء الاختبار الوسيط.

نتائج التحقق: `npm run typecheck` ✅، `npm test` ✅ (80/80)، `npm run lint` ✅ دون أخطاء مع 36 تحذيرًا قائمًا، و`npx vite build` ✅.


## 11 — اختبار فعلي وإصلاح مسار كرت الأدوار (11 سبتمبر 2026)

تم الدخول فعليًا إلى النشر الإنتاجي بحساب الاختبار `773303455` والتحقق من صفحة الإعدادات. ظهرت البطاقة أولًا بحالة تحميل الصلاحيات، ثم تحولت إلى روابط «قائمة الموظفين» و«الأدوار وصلاحيات الشاشات» بعد اكتمال الجلب، وظهرت حالة «أنت مدير النظام بالفعل» بصورة صحيحة. كُشف خلل فعلي عند الضغط على بطاقة الأدوار: العنوان تغيّر إلى `/settings/access-control` لكن محتوى صفحة الإعدادات بقي ظاهرًا، بسبب تعريف المسار كمسار فرعي دون إخراج محتوى الابن عبر `Outlet`. أضيف غلاف مسار يعرض `Outlet` للمسارات الفرعية مع إبقاء صفحة الإعدادات للجذر فقط.

التحقق البرمجي بعد الإصلاح: `npm run typecheck` ✅، `npm test` ✅، `npm run lint` ✅ دون أخطاء مع التحذيرات القائمة، و`npx vite build` ✅. يلزم التحقق النهائي من النشر بعد دفع هذا الإصلاح.


## 12 — توجيه الموظف إلى الشاشات المحددة فقط (11 سبتمبر 2026)

تم تعديل منطق الوصول بحيث لا تُعدّ الرئيسية شاشة عامة للموظف الذي لديه صلاحيات دور محدد. تُخفى الرئيسية من التنقل، ويُوجَّه الموظف عند دخوله إلى أول شاشة مسموحة له حسب دوره. إذا لم يُمنح أي شاشة، تظهر رسالة واضحة دون رابط يعيده إلى حلقة لا نهائية. يبقى استثناء التمهيد للحساب غير المحمّل، كما يبقى مدير النظام قادرًا على رؤية الرئيسية. أضيفت اختبارات تمنع الرئيسية لموظف يملك `reports.read` وتتحقق من اختيار أول شاشة مسموحة.

نتائج التحقق: `npm run typecheck` ✅، `npm test` ✅ (77/77)، `npm run lint` ✅ دون أخطاء مع 36 تحذيرًا قائمًا، و`npx vite build` ✅.


## 13 — إيقاف مؤقت لتسجيل الدخول والموظفين والأدوار + تفقيط السندات (21 سبتمبر 2026)

**قرار صاحب المشروع:** إيقاف تسجيل الدخول، وإيقاف شاشات الموظفين والأدوار والصلاحيات، **مؤقتًا وبدون حذف**: يختفي كل ما يتعلق بهما من الشاشة، ويبقى الكود كاملًا لإعادة التفعيل لاحقًا.

**آلية الإيقاف (مصدر واحد للحقيقة):** ملف جديد `src/lib/features.ts` يحمل مفتاحين: `AUTH_REQUIRED` و`ACCESS_CONTROL` (كلاهما `false` حاليًا). إعادة التفعيل بإرجاع القيمة إلى `true` فقط — لا تعديل آخر في أي ملف.

**ما تغيّر بالتفصيل:**
- `src/routes/__root.tsx`: عند إيقاف المصادقة يُعرض التطبيق مباشرة داخل `AppShell` بدون `SignInGate` وبدون شاشة دخول، مع بقاء كود الشاشة والبوابة كما هو.
- `src/lib/auth/verify.server.ts`: `requireUserId()` تعيد هوية الجهاز الثابتة **`dev-user`** (نفس الهوية المعتمدة مسبقًا في `DEV_USER_ID` و`DEV_USER`) بدل رفض الطلب. تجنّبنا تعريف هوية جديدة كي لا تتعارض كتابات الخادم مع ما تراه الواجهة.
- `src/lib/auth/client.ts`: `authEnabled` تحترم الآن مفتاح الوحدة، فأصبحت الواجهة والخادم متفقين على أن الجهاز غير مسجَّل الدخول (سابقًا كان الخادم يعاملك كـ dev-user والواجهة تعاملك كغير مسجَّل — تناقض يظهر في شاشة الإعدادات).
- `src/server/permissions.ts`: `requirePermission()` تمرّر الطلب دون فحص دور، و`listUserPermissions()` تعيد قائمة فارغة، فلا حجب ولا رسائل صلاحيات على الخادم أو في الواجهة.
- `src/components/app-shell.tsx`: عند الإيقاف لا تُصفّى القوائم ولا يُمنع أي مسار (كل الشاشات ظاهرة للجميع).
- `src/components/settings/access-control-card.tsx`: تُرجع `null` عند الإيقاف، وتُخفيها صفحة الإعدادات صراحةً أيضًا.
- `src/routes/employees.tsx` و`src/routes/settings.access-control.tsx`: حارس `beforeLoad` يعيد التوجيه إلى `/settings`، فالشاشتان لا تُفتحان حتى بالرابط المباشر.
- `src/routes/settings.tsx`: بطاقة «الحساب» تستبدل زر الخروج وزر «تفعيل حسابي كمدير النظام» برسالة توضيحية عن الوضع المؤقت (لا يوجد دخول في هذه المرحلة).

**تحسين إضافي (تفقيط السندات):** أُضيف `src/lib/numbers-ar.ts` (تفقيط عربي كامل بلا تبعيات خارجية)، وصار `VoucherPrintTemplate.tsx` يمرّر `amountInWords` إلى قالب السند الذي كان يدعم الحقل أصلًا دون أن يملؤه أحد. النتيجة: يُطبع في السند «مبلغ وقدره … ريال يمني فقط لا غير».

**⚠️ أثر أمني يجب أن يعرفه صاحب المشروع:** ما دام `AUTH_REQUIRED = false` فإن أي شخص يملك رابط النظام يدخله بكامل الصلاحيات على البيانات الحقيقية. هذا استثناء مؤقت مقصود سُجّل أيضًا كملاحظة تشغيلية في أعلى `ROADMAP.md`، ولم تُحذف أو تُعدّل أي مرحلة من مراحل الخطة. يجب إعادة التفعيل قبل أي استخدام جماعي أو الاعتماد الكامل على النظام.

**تقرير تفصيلي بالحالة والخطة:** `docs/PROJECT_REPORT_AND_PLAN_2026-09-21.md` — يشرح ما أُنجز، وتحذيرات التناقض، وما تبقّى من طلبات صاحب المشروع (الفحص الشامل، تطوير التقارير والسندات والفواتير، إعادة تصميم القوالب، حذف فاتورة المبيعات لصالح خدمة التطريز، وتبسيط المخزون إلى إدخال/إخراج/متبقٍ)، مع خطة مرحلية مرتّبة.

**نتائج التحقق (تشغيل فعلي):** `npm run typecheck` ✅ (0 أخطاء) · `npm test` ✅ (77/77، 23 مجموعة) · `npm run lint` ✅ (0 أخطاء، 36 تحذيرًا قائمًا) · `npm run build` ✅ (بناء ناجح + Service Worker).


## 14 — تنفيذ الخيار (ب): إخفاء فواتير بيع/شراء البضاعة من الواجهة (21 سبتمبر 2026)

**قرار صاحب المشروع:** اختار الخيار **(ب) إخفاء من الواجهة فقط** — لا استبدال كامل ولا إعادة تسمية للنموذج، مع بقاء المنطق كما هو ليُعاد إظهاره لاحقًا.

**آلية الإخفاء:** مفتاح ثالث في `src/lib/features.ts` باسم `PRODUCT_SALES = false`. إعادة الإظهار بإرجاع القيمة إلى `true` فقط.

**ما تغيّر (واجهة فقط — لا تغيير في أي منطق محاسبي أو مخزني):**
- `src/routes/sales.tsx`: يُخفى زرّا «بيع بضاعة» و«مشتريات»، ويبقى زر «خدمة تطريز» بشكل أساسي بعنوان «فاتورة خدمة تطريز». عنوان الشاشة ووصفها يتغيران إلى «خدمات التطريز»، والتصفية تُخفي زر «مشتريات»، ونص الحالة الفارغة يصبح «ابدأ بفاتورة خدمة تطريز».
- `src/components/app-shell.tsx`: اسم شاشة `/sales` في القائمة الرئيسية يصبح «خدمات التطريز»، وزر الإجراءات السريعة في الهاتف يصبح «فاتورة خدمة تطريز».

**ما لم يتغير عمدًا:** كل الفواتير القديمة (بيع/شراء) تبقى ظاهرة في القائمة وقابلة للفتح والتعديل والطباعة والاعتماد والإلغاء والحذف، وتبقى دوال الإدخال والقيود (`accounting.ts`، `domain/operations.ts`، `application/mutate.ts`) والخادم كما هي تمامًا. أي إرجاع للمفتاح يعيد واجهة المبيعات كما كانت دون أي تعديل آخر.

**نتائج التحقق (تشغيل فعلي):** `npm run typecheck` ✅ · `npm test` ✅ (77/77) · `npm run lint` ✅ (0 أخطاء، 36 تحذيرًا قائمًا) · `npm run build` ✅.


## 15 — المرحلة أ: التدقيق الشامل والإصلاحات المحاسبية (21 سبتمبر 2026)

**التقرير المفصّل:** `docs/AUDIT_2026-09-21.md` — كل بند فيه مُتحقَّق منه بالكود أو بفحص قابل للتشغيل.

### المشاكل الحرجة التي أُصلحت

1. **المبلغ المدفوع كان يُهمَل في ذمة الطرف (خطأ محاسبي حرج).** كان يُكتب للفاتورة قيدان (ذمة بالإجمالي + صندوق بالمدفوع) بلا قيد يُقاصّ المدفوع من الذمة، فكان رصيد العميل/المورد يعود إلى **إجمالي الفاتورة** بعد كل تحديث من الخادم (بخلاف ما يظهر لحظة الحفظ). الإصلاح: مصدر حقيقة واحد `src/domain/invoice-ledger-rows.ts` يستخدمه `saveInvoice` و`syncLegacyData`، + هجرة `0024_invoice_balance_settlement.sql` تُصلح الفواتير القديمة في الإنتاج بأمان وقابلية إعادة تنفيذ.
2. **سلسلة الهجرات كانت تتعطل عند 0019** فلا تُطبَّق 0020–0024 على أي قاعدة جديدة (تعذّر بناء قاعدة من الصفر / خطة استعادة): سببَان — `revoke ... from anon, authenticated` على قاعدة بلا دورَي Supabase، و`alter function public.bump_sync_version()` ودالة غير مُنشأة في أي هجرة (يُرجّح أنها كانت في `0009` المفقودة). أُصلح الملف ليصبح محمولًا (portable) دون تغيير أثره على Supabase. **مطلوب تحقّق على الإنتاج:** `select name from public._migrations order by name;` للتأكد من وجود 0020–0024.
3. **حُذفت نسخة محاسبية ميتة مكرّرة** (`applyInvoice/applyVoucher/applyExpense` في `src/lib/accounting.ts`) كانت لا تُنفَّذ من أي شاشة ومتباعدة عن النسخة الحيّة — وهي التي سمحت بحدوث الخطأ (1) في مكان دون الآخر. تراجعت تحذيرات lint من 36 إلى 32.
4. **فقدان معلومات المستند بعد إعادة الجلب:** من `src/lib/store.ts` صارت طريقة الدفع ورقم المستند تُشتقّان من جدول المستند، ونوع المستند والبيان يُؤخذان من صف المستند الأصلي (`_party`) لا من صف السداد (كان صف `invoice_payment` يُصنّف الفاتورة «مصروفًا» أحيانًا).

### فحص محاسبي دائم جديد

`npm run check:accounting` — يبني مخطط قاعدة البيانات كاملًا من `migrations/*.sql` في PGlite، ثم يتحقق من 18 ثابتًا محاسبيًا (بيع نقدي/آجل/جزئي، مشتريات، سندات قبض وصرف، حذف مستند، وأمان هجرة 0024). النتيجة الحالية: **كل الثوابت سليمة**.

### ما بقي مفتوحًا من التدقيق (مصنّف في التقرير)

نطاق الدفتر (ذمم وصندوق الآن، وليس مزدوجًا كاملًا — لا قيود إيراد/تكلفة)، تحميل المصروفات على حساب `purchases` بدل حسابات مصروفات حسب الفئة، غياب تحقّق خادمي من مجاميع بنود الفاتورة، الحذف النهائي للفواتير بلا سجل كافٍ، ورقم الهجرة `0009` المفقود.

**نتائج التحقق (تشغيل فعلي):** `npm run typecheck` ✅ · `npm test` ✅ (77/77) · `npm run lint` ✅ (0 أخطاء، 32 تحذيرًا) · `npm run build` ✅ · `npm run check:accounting` ✅ (18/18).


## 16 — المرحلة ب: تبسيط المخزن إلى إدخال/إخراج/متبقٍ (21 سبتمبر 2026)

**مفتاح جديد:** `FEATURES.ADVANCED_INVENTORY = false` في `src/lib/features.ts` — إرجاعه إلى `true` يعيد الوضع المتقدم كما كان (لا حذف لأي شيء).

**شاشة المخزن الآن:**
- زرّان رئيسيان: **إدخال بضاعة** و**إخراج بضاعة**، وزر ثانوي **مادة جديدة**.
- **إدخال بضاعة** يُحدّث المخزون **فورًا** (بلا خطوة مطابقة المدير)، والاسم الجديد يُنشأ كمادة تلقائيًا — وهذا إصلاح فقدان بيانات:
  في السلوك السابق كان أي صنف يُكتب بالاسم دون اختياره من القائمة لا تُسجَّل له حركة مخزون إطلاقًا (لأن الحركة تحتاج معرّف مادة) فتضيع الكمية.
- **إخراج بضاعة** يخصم من المتوفر فورًا، ويمنع الخادم أي إخراج أكبر من الرصيد المتاح في المخزن.
- **ثلاث بطاقات ملخص**: عدد الأصناف، إجمالي الكميات المتبقية، وعدد الأصناف الواصلة للحد الأدنى (مع تنبيه أحمر).
- **تعديل الكمية من بطاقة المادة مُخفى** في الوضع المبسّط مع رسالة توضح أن الكمية تتغير من إدخال/إخراج فقط (كان تغيير الكمية مباشرة يجعل العرض يخالف حركات المخزون المسجّلة حتى التحديث التالي).

**تحسين محاسبي مرافق:** المستند بلا قيمة مالية (توريد/صرف مخزني بأسعار صفرية) لم يعد يُنشئ قيودًا بقيمة صفر في `financial_transactions` تُشوّش دفتر القيود والتقارير.

**تحسين في بوابات التحقق:** `npm test` كان يشغّل 8 ملفات اختبار فقط ويُسقط ثلاثة ملفات موجودة في المشروع ومنها **اختبار المحاسبة الأساسي** `src/domain/operations.test.ts` و`architecture.guard.test.ts` و`permissions.test.ts`. أُضيفت الثلاثة إلى الأمر بعد التأكد من نجاحها: **107/107 اختبارًا** بدل 77. (تراجعت تحذيرات lint أيضًا إلى 30 بعد حذف استيرادات ميتة.)

**نتائج التحقق (تشغيل فعلي):** `npm run typecheck` ✅ · `npm test` ✅ (107/107) · `npm run lint` ✅ (0 أخطاء، 30 تحذيرًا) · `npm run build` ✅ · `npm run check:accounting` ✅ (18/18).


## 17 — المرحلة ج: تحسين منطق الإدخال في فاتورة التطريز والسندات (21 سبتمبر 2026)

### 17.1 إدخال مبالغ وكميات صارم (إصلاح خطر إدخال حقيقي)

**المشكلة:** كل حقول الأرقام كانت تُقرأ بـ `parseFloat` التي **تُسقط ما بعد الرقم بصمت**: كتابة `12س` تُحفظ 12، و`1.2.5` تُحفظ 1.2 — أي مبلغ/كمية غير التي كتبها المستخدم بلا أي تحذير.

**الإصلاح:** دالة مشتركة `parseAmountStrict` في `src/lib/utils.ts` مع رسائل عربية موحّدة (`amountInputError`): تقبل الأرقام الإنجليزية والعربية (٠-٩ و٫) وتفصل الآلاف بالفاصلة، وترفض أي نص مخلوط أو أكثر من نقطة عشرية أو قيمة سالبة. مطبّقة الآن في: بند فاتورة الخدمة (الكمية/السعر)، الخصم، المدفوع، ومبلغ السند. مع اختبارات وحدة جديدة: `src/lib/money-input.test.ts` (4 مجموعات).

### 17.2 اختيار الطرف بالبحث + الرصيد قبل وبعد المستند

- في فاتورة خدمة التطريز وفي السند: قائمة الطرفين صارت `AppSelect` قابلة للبحث بالاسم أو الرقم وتُظهر رصيد كل طرف بجانب اسمه (كانت `select` عادية تكبر مع عدد العملاء).
- حين يُختار الطرف، يظهر سطر مباشر: **الرصيد الحالي → الرصيد بعد هذا المستند**، فيرى المحاسب النتيجة قبل الحفظ.

### 17.3 تحسينات إدخال أخرى

- **إدخال سريع بالكيبورد:** زر Enter في أي حقل بند يُضيف البند فورًا (بلا نزول إلى زر الإضافة).
- **رسالة خطأ صحيحة:** كانت رسالة الحفظ في شاشة المبيعات تقول «اختر المورد» حتى في فاتورة العميل — صارت «اختر العميل».
- **بيان تلقائي للسند:** إن تُرك البيان فارغًا يُكتب تلقائيًا «قبض من …» أو «صرف إلى …» بدل سند بلا بيان في الطباعة والقيود.
- **تفقيط مباشر أثناء الكتابة:** تحت حقل مبلغ السند يظهر المبلغ كتابةً بالعربية (نفس نص السند المطبوع) للتأكد قبل الحفظ.
- **وضوح الرصيد في الفاتورة:** تسمية «الاجمالي الكلي» صارت «الرصيد بعد الفاتورة» (وهو معناه الفعلي في المعادلة).

**نتائج التحقق (تشغيل فعلي):** `npm run typecheck` ✅ · `npm test` ✅ (111/111) · `npm run lint` ✅ (0 أخطاء، 30 تحذيرًا) · `npm run build` ✅ · `npm run check:accounting` ✅ (18/18).
