# جلسة 25 سبتمبر 2026 — جلب تحديثات GitHub ودمجها، وبيان ما تبقّى من الخطة

> ملحق مؤرَّخ لـ `PROJECT_STATUS.md`. (تعذّر إدراجه داخله مباشرة لأن أداة التحرير فشلت في مطابقة النص العربي في ذلك الملف — يُنصح بدمجه في §24 عند أول فرصة.)

## 1. الحالة قبل الجلسة

- الفرع المحلي `main` كان على `d1cd99b`، والمستودع البعيد متقدّم بـ**7 التزامات** على `origin/main` ينتهي بـ`1343b7a`.
- في مجلد العمل كانت تعديلات **غير مدفوعة** من جلسة فحص الجوال (13 ملفًا) — ليست في GitHub إطلاقًا.

### التزامات المستودع التي جُلبت

```
1343b7a fix(performance): prevent cloud sync from blocking screens
53ef291 fix(ui): normalize database dates for reliable navigation
59b2541 fix(pwa): refresh stale service worker shell
6c76774 fix(ui): keep mobile layout on desktop-mode browsers
ad904a6 chore(deploy): reload production database environment
2f2c89e feat(system): unify cashbox sync and document workflows
a222fc4 perf(offline): preload screens and persist initial data
490024e fix(dev): allow preview host for navigation testing
02b61b5 perf(nav): instant screen transitions
```

## 2. ما نُفّذ (بلا فقدان أي عمل)

1. حُفظ العمل المحلي غير المدفوع في `stash@{0}` كشبكة أمان (بقي محفوظًا بعد الدمج).
2. `git merge --ff-only origin/main` — تقدّم سريع إلى `1343b7a`.
3. `git stash pop` ثم حلّ **تعارضين حقيقيين فقط**:
   - `src/components/app-shell.tsx`: قُبل **المنطق المحلي** (`cloudState` بحالة «جارٍ التحقق…») لأن أسطرًا أخرى غير متعارضة دمجها git تلقائيًا تعتمد عليه بالفعل، مع **الاحتفاظ بتحسين البعيد** (مدة `transition` = 0.1) و**الاحتفاظ بإصلاح الجوال المحلي** للوحة التنبيهات (`fixed inset-x-3 … sm:absolute`) و`SCREEN_MODULES` الذي أعاده البعيد.
   - `src/routes/settings.tsx`: قُبلت **نسخة المستودع** (`typeof navigator !== "undefined" ? …`) — نفس الأثر وأكثر أمانًا.
4. لا يوجد أي موضع تعارض متبقٍ (`grep` على علامات التعارض = صفر).

## 3. التحقق بعد الدمج (تشغيل فعلي)

- `npm run typecheck` ✅ (0 أخطاء)
- `npm test` ✅ **136/136** اختبارًا
- `npm run lint` ✅ 0 أخطاء · **26 تحذيرًا** (كانت 38 — تحسّنت)
- كل الشاشات على خادم التطوير: `/`, `/sales`, `/vouchers`, `/inventory`, `/cashbox`, `/expenses`, `/parties`, `/reports`, `/settings` → **HTTP 200** بمحتوى فعلي، وبلا أي أثر لأخطاء عرض أو ترطيب في HTML المُصيَّر.
- **لم يُدفع شيء:** تعديلات الجوال المحلية ما زالت **غير مُلتَزَمة** في مجلد العمل وتحتاج التزامًا ودفعًا.

## 4. بنود أُغلقت وكانت مفتوحة سابقًا

| البند | الدليل |
|---|---|
| ملف قفل التبعيات المزدوج | لم يبقَ إلا `package-lock.json` — `bun.lock` لم يعد موجودًا |
| `metadata.json` | لم يعد موجودًا في الجذر |
| التحقق الخادمي من مجاميع بنود الفاتورة | `validateInvoiceMath` في `src/server/repository.ts` |
| أثر حذف المستندات | `src/domain/audit.ts` مستخدم في `src/server/repository.ts` |
| `AGENTS.md` / `screenshots/` | `AGENTS.md` باقٍ بوظيفته، و`screenshots/` غير مُتعقَّب في Git |

## 5. البنود المتبقية فعليًا (مُتحقَّق منها بالكود)

| # | البند | المرجع | الحالة |
|---|---|---|---|
| 1 | إعادة تفعيل الدخول والأدوار والصلاحيات (مرحلة «و») | `ROADMAP.md` م1 · `plan §5` | ❌ `FEATURES.AUTH_REQUIRED=false` و`ACCESS_CONTROL=false`. **النظام مفتوح لأي من يملك الرابط على بيانات حقيقية.** |
| 2 | متغيرات Vercel للمصادقة (`VITE_AUTH_ENABLED`, `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`, `DATABASE_URL`) | `ROADMAP.md` م1 | ❌ إعداد يدوي من المالك |
| 3 | نسخ احتياطي يومي مجدول لقاعدة Neon | `ROADMAP.md` م1 | ❌ غير مُجدوَل |
| 4 | تدقيق سجل Git للأسرار + تدوير/حذف مفتاح Firebase المكشوف تاريخيًا | `ROADMAP.md` م1 | ❌ لم يُنفَّذ |
| 5 | اختبار سيناريو تعارض المزامنة على جهازين | `ROADMAP.md` م2 | ❌ لم يُنفَّذ |
| 6 | اختبار أوفلاين ميداني حقيقي على هاتف أمين المخزن | `ROADMAP.md` م2 | ❌ لم يُنفَّذ |
| 7 | مصير `src/lib/multiplayer/p2p.ts` | `ROADMAP.md` م2 | ❌ **لا يُستورد في أي ملف** — وحدة يتيمة (تحقّق: `grep` = صفر استيراد) |
| 8 | اختبارات مسارات الصلاحيات الحرجة | `ROADMAP.md` م2 | ⚠️ موجودة لكن **غير قابلة للتشغيل فعليًا** ما دام فحص الصلاحيات موقوفًا |
| 9 | ربط مراقبة أخطاء في الإنتاج (Sentry أو ما يعادله) | `ROADMAP.md` م3 | ❌ لا وجود لأي تكامل في `src/` أو `package.json` |
| 10 | خطة رجوع (Rollback) موثّقة | `ROADMAP.md` م3 | ❌ لا وجود لها |
| 11 | تدريب المستخدمين + ترحيل البيانات القديمة + تثبيت PWA على أجهزة حقيقية | `ROADMAP.md` م3 | ❌ لم يُنفَّذ |
| 12 | إطلاق تدريجي + جمع ملاحظات + مراجعة أداء تحت حمل حقيقي | `ROADMAP.md` م4 | ❌ لم يبدأ |
| 13 | **تصدير CSV/Excel من التقارير** | `plan §5` (د/4، هـ/3) | ❌ لا وجود لأي تصدير CSV في `src/` (PDF موجود عبر `PrintPreview`) |
| 14 | تقرير **حركة صنف بين تاريخين برصيد افتتاحي/ختامي** | `plan §5` (ب/4) | ❌ الموجود «الأصناف الأكثر حركة» فقط، بلا رصيد افتتاحي/ختامي |
| 15 | تصفية موحّدة (فترة + جهة + صنف) + **حفظ آخر تصفية** | `plan §5` (هـ/2) | ❌ لا حفظ للتصفية (لا `localStorage` لها) |
| 16 | تنظيف تحذيرات lint | `ROADMAP.md` م2 | ⚠️ تراجعت إلى **26 تحذيرًا** (0 أخطاء) — منخفضة الأولوية |
| 17 | فجوة ترقيم الهجرة `0009` | `ROADMAP.md` م0 | ❌ ما زالت غائبة (القفزة 0008 → 0010) |
| 18 | المعاينة البصرية النهائية للطباعة على ورق A4 حقيقي | `PROJECT_STATUS.md` §22.7 | ⚠️ تحتاج عين المالك |

## 6. الخلاصة

طلباتك الأصلية (إيقاف الدخول، إيقاف الموظفين والأدوار، الفحص الشامل، تبسيط المخزون،
تطوير التقارير والسندات والفواتير، القوالب والمعاينة قبل الطباعة، إعادة تصميم الشاشات،
إخفاء فاتورة المبيعات لصالح خدمة التطريز) **منجزة كلها**. المتبقي فعليًا هو:

1. **إعادة تفعيل الدخول والصلاحيات** — أكبر فجوة، وأمنية قبل أن تكون وظيفية.
2. **بنود الجاهزية التشغيلية**: النسخ الاحتياطي، المراقبة، خطة الرجوع، التدريب، والاختبار الميداني على جهازين.
3. **تحسينات تقارير مؤجلة**: تصدير CSV، حركة صنف برصيد افتتاحي/ختامي، وحفظ التصفية.
4. **تنظيفات صغيرة**: `p2p.ts`، فجوة `0009`، و26 تحذير lint.
